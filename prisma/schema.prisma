datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model Entry {
  id        Int      @id @default(autoincrement())
  category  String
  content   String
  createdAt DateTime @default(now())
}
